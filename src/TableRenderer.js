class TableRenderer {
  constructor(userMap, readMeFile) {
    this.userMap = userMap;
    this.readMeFile = readMeFile;
  }

  render(userName, addedFiles, deletedFiles) {}
}

module.exports = TableRenderer;
